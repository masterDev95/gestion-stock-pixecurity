import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gestion-stock-pixecurity',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
